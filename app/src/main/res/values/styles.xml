<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/blue3</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="preferenceTheme">@style/customPreferenceTheme</item>
    </style>

    <style name="customPreferenceTheme" parent="PreferenceThemeOverlay">
        <item name="android:textColorPrimary">#ffffff</item>
        <item name="android:textColorSecondary">#ffffff</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="forecast_table_text_view">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:minHeight">30dp</item>
        <item name="android:gravity">center</item>
        <item name="android:padding">3dip</item>
        <item name="android:textColor">@color/grey</item>
        <item name="android:text">@string/label_no_information_yet</item>
    </style>

    <style name="forecast_table_text_view_header" parent="forecast_table_text_view">
        <item name="android:textColor">@color/white</item>
    </style>


    <style name="WhiteTextAppearance" parent="@android:style/TextAppearance.Large">
        <item name="android:textColor">@color/white</item>
    </style>

</resources>
